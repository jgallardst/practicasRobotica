
# Sources set
SET ( SOURCES
  specificworker.cpp
  specificmonitor.cpp
  TelegramHandler.cpp
)

# Headers set
SET ( HEADERS
  specificworker.h
  specificmonitor.h
  TelegramHandler.h
)

find_package(OpenMP)
if (OPENMP_FOUND)
    set (CMAKE_C_FLAGS ${CMAKE_C_FLAGS} ${OpenMP_C_FLAGS})
    set (CMAKE_CXX_FLAGS ${CMAKE_CXX_FLAGS} ${OpenMP_CXX_FLAGS})
endif(OPENMP_FOUND)
message(STATUS "OPENMP: ${OPENMP_FOUND}")

INCLUDE( $ENV{ROBOCOMP}/cmake/modules/opencv3.cmake )
SET(CMAKE_CXX_FLAGS " -opencv_core -opencv_highgui ")
set(CMAKE_CXX_STANDARD 17)

